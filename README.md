# Streamlit dashboard

Run locally:

```bash
pip install -r requirements.txt
streamlit run dashboard.py
```

If deploying the entire project from the parent folder, use `Code Repository/dashboard.py` as the main file.

This app only visualises pre-rendered files in `outputs/` (raw inputs in `data/` are ignored for deployment).
# millenium-drought-dashboard
